<blockquote>
    <small>{nick} @ {when}</small>
    <p><span class="label {class}">{name}</span></p>
    {?voteid}
        <ul>
            {#choices}
              <li><a class="btn btn-mini btn-primary" onclick="vote('{voteid}','{.}')">{.}</a>
                    <div id="vote-responses-for-vote-{voteid}-{.}"></div>
              </li>{~n}
            {/choices}
        </ul>
 
    {/voteid}
</blockquote>
